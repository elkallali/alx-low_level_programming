#include "main.h"

/**
 * mul - function that checks for a digit (0 through 9).
 *
 * @a: first number
 * @b: second number
 *
 * Return: multiplie of @a * @b.
 */
int mul(int a, int b)
{
		return (a * b);
}
